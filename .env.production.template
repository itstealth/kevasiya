# =============================================================================
# KEVASIYA PRODUCTION ENVIRONMENT VARIABLES
# =============================================================================
# IMPORTANT: Generate new secrets before deployment!
# Run: openssl rand -base64 32
# =============================================================================

# ========================
# MAIN APPLICATION (kevasiya.com)
# ========================

# Database
MYSQL_ROOT_PASSWORD=<GENERATE_STRONG_PASSWORD>
DB_PASSWORD=<GENERATE_STRONG_PASSWORD>

# JWT Secret (run: openssl rand -base64 32)
JWT_SECRET=<GENERATE_STRONG_PASSWORD>

# Instagram API (regenerate these!)
INSTAGRAM_APP_SECRET=<GENERATE_NEW_SECRET>
INSTAGRAM_ACCESS_TOKEN=<GENERATE_NEW_TOKEN>
INSTAGRAM_CORPORATE_ACCESS_TOKEN=<GENERATE_NEW_TOKEN>

# Webhook Secret
WEBHOOK_SECRET=<GENERATE_STRONG_PASSWORD>

# ========================
# STRAPI CMS
# ========================

# Database
STRAPI_DB_PASSWORD=<GENERATE_STRONG_PASSWORD>

# JWT Secrets (run: openssl rand -base64 32)
STRAPI_JWT_SECRET=<GENERATE_STRONG_PASSWORD>
STRAPI_ADMIN_JWT_SECRET=<GENERATE_STRONG_PASSWORD>

# App Keys (run: openssl rand -base64 32)
STRAPI_APP_KEYS=<COMMA_SEPARATED_KEYS>
STRAPI_TOKEN_SALT=<GENERATE_STRONG_PASSWORD>
STRAPI_TRANSFER_TOKEN_SALT=<GENERATE_STRONG_PASSWORD>
STRAPI_ENCRYPTION_KEY=<GENERATE_STRONG_PASSWORD>

# ========================
# GENERATE NEW SECRETS SCRIPT
# =============================================================================
# Run this to generate all secrets:
#
# echo "MYSQL_ROOT_PASSWORD=$(openssl rand -base64 24 | tr -dc 'a-zA-Z0-9' | head -c 32)"
# echo "DB_PASSWORD=$(openssl rand -base64 24 | tr -dc 'a-zA-Z0-9' | head -c 32)"
# echo "JWT_SECRET=$(openssl rand -base64 32)"
# echo "STRAPI_DB_PASSWORD=$(openssl rand -base64 24 | tr -dc 'a-zA-Z0-9' | head -c 32)"
# echo "STRAPI_JWT_SECRET=$(openssl rand -base64 32)"
# echo "STRAPI_ADMIN_JWT_SECRET=$(openssl rand -base64 32)"
# echo "STRAPI_APP_KEYS=$(openssl rand -base64 32),$(openssl rand -base64 32),$(openssl rand -base64 32),$(openssl rand -base64 32)"
# echo "STRAPI_TOKEN_SALT=$(openssl rand -base64 32)"
# echo "STRAPI_TRANSFER_TOKEN_SALT=$(openssl rand -base64 32)"
# echo "STRAPI_ENCRYPTION_KEY=$(openssl rand -base64 32)"
# echo "WEBHOOK_SECRET=$(openssl rand -base64 32)"
# =============================================================================
